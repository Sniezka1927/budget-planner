const BudgetPage = () => {
  return (
    <>
      The Budget page would allow the user to set a budget for each category and
      track their progress. The page would display a chart showing the user's
      spending for each category compared to their budget.
    </>
  );
};

export default BudgetPage;
